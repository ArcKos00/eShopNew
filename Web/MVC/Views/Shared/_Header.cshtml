@model IEnumerable<MVC.ViewModels.Header>

<div class="esh-header">
    <div class="container">
        @foreach (var header in @Model)
        {
            <a class="esh-header-back" asp-area="" asp-controller="@header.Controller" asp-actions="Index">@header.Text</a>
        }
    </div>
</div>